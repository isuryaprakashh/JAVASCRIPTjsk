// named export - exporting multiple items per file

const sum = (a,b) => {
    return a+b
}
const mul = (a,b) => {
    return a*b
}

const text = "K L University"

const object = {
    id:30929,
    name:"KLU"
}

export {sum,mul,text,object}